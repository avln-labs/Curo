# CURO Backend

This is the API server scaffold for CURO.

## Run locally

1. Copy `.env.example` to `.env`
2. Install dependencies: `npm install`
3. Start in development: `npm run dev`

The backend exposes feature-based routes under `/api/v1`.
