# CURO Frontend

This is the React frontend scaffold for CURO.

## Run locally

1. Install dependencies: `npm install`
2. Start dev server: `npm run dev`

The app is configured to proxy `/api` requests to `http://localhost:4000`.
